name: vault_01
description: A secure vault application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Your Rust bridge dependency
  rust_lib_vault_01:
    path: rust_builder
  
  # State management
  flutter_riverpod: ^2.4.0
  
  # Navigation
  go_router: ^12.0.0
  
  # UI utilities
  flutter_hooks: ^0.20.0
  
  # Functional programming utilities
  fpdart: ^1.1.0
  flutter_rust_bridge: ^2.11.1
  path_provider: ^2.1.5
  hooks_riverpod: ^2.6.1
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true